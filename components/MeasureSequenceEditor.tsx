import React from 'react';

// This component and its related 'Advanced Sequence' functionality have been removed.
// It is kept as a null component to signify its removal from the app.
const MeasureSequenceEditor: React.FC = () => null;

export default MeasureSequenceEditor;
