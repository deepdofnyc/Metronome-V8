import React from 'react';

// This component is obsolete and has been replaced by SetlistManager.
// It is kept as a null component to avoid breaking any potential (though unlikely) old references
// and to signify that its functionality has been migrated.
const PlaylistManager: React.FC = () => null;

export default PlaylistManager;